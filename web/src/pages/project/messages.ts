import { defineMessages } from 'react-intl';

const messages = defineMessages({
  loading: {
    id: 'PROJECT.LOADING',
    defaultMessage: 'Loading project...',
    description: 'Loading state message shown while project data is being fetched',
  },
});

export default messages;
